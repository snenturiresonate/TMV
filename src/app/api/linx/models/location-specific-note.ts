/* tslint:disable */
export class LocationSpecificNote {
  note?: string;
  noteType?: string;

  constructor(note: string, noteType: string) {
    this.note = note;
    this.noteType = noteType;
  }
}
